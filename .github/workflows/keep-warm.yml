name: keep-warm

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Render-Backend warmhalten
        run: curl -fsS -o /dev/null -w "health %{http_code} in %{time_total}s\n" https://harekrishnaai.onrender.com/health